INSTALL
------------
Place script (Sparkup.vbs) in the Script\VBScript\-subdirectory of the PSPad folder.

USAGE
------------
Select a Shortcut (e.g. "div#wrapper > div#header > img < div#main > ol > ul.item-$*3 < < div#footer > p")
and press Ctrl+, or select "Sparkup Expand" in the "Scripts"-Menu.